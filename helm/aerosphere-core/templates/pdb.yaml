{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "aerosphere-core.fullname" . }}
  labels:
    {{- include "aerosphere-core.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "aerosphere-core.selectorLabels" . | nindent 6 }}
{{- end }}
