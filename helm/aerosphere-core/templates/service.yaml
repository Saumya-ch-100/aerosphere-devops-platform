apiVersion: v1
kind: Service
metadata:
  name: {{ include "aerosphere-core.fullname" . }}
  labels:
    {{- include "aerosphere-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aerosphere-core.selectorLabels" . | nindent 4 }}
