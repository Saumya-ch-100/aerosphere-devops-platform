groups:
- name: aerosphere-alerts
  rules:
  - alert: HighErrorRate
    expr: job:request_error_rate:rate5m > 0.05
    for: 5m
    labels:
      severity: critical
    annotations:
      summary: High error rate detected
